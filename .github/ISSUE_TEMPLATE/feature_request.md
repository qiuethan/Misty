---
name: Feature or task
about: New capability, improvement, or a chunk of planned work
title: ''
labels: ''
assignees: ''
---

Blocked by:

<!-- ^ List the issue numbers this depends on, on that ONE line above, like
     `#40, #42`. The label automation reads them from that line and keeps the
     `blocked` / `ready` labels in sync as those issues close and reopen.
     Delete the line entirely if nothing blocks this. -->

## What

<!-- The capability, in one or two sentences. -->

## Why

<!-- The problem it solves. For a student org with rotating leadership, "who is
     worse off today without this, and how" is the useful framing. -->

## Where

<!-- Delete the ones that don't apply. Work spanning several zones is usually
     better split into one issue per zone — PRs are expected to stay single-zone. -->

`discord-bot` · `packages/auth` · `services/connectors` · `services/documentation-system` · `services/llm` · `services/meeting` · `services/team-tracking` · `services/verification` · `docs` · `scripts` · `.github`

## Done looks like

<!-- Concrete and checkable. A reviewer should be able to tell whether this
     shipped without asking you. -->

- [ ]
- [ ]

## Design notes

<!--
Optional. Worth writing if any of these apply:
  - It needs a new endpoint, scope, or config variable.
  - It needs a migration.
  - It crosses a service boundary, or adds a dependency between services.
  - It changes something an existing consumer relies on.
  - It conflicts with a documented convention — say which, and why it's worth it.
-->

## Out of scope

<!-- Optional, but it's the field that most often prevents scope creep. -->
