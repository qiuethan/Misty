services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: docs
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: docs
    ports:
      - "5434:5432"
    volumes:
      - docs_pgdata:/var/lib/postgresql/data
volumes:
  docs_pgdata:
